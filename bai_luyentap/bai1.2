package main

import "fmt"

func findNthTerm(N int) int {
	N = ((1 + N) * N) / 2
	return N
}

func main() {
	// Không nhập hay in ra Console : trong func không nên có fmt
	fmt.Println(findNthTerm(4))
}
